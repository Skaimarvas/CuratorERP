# Auth Feature

Use this feature for login, session bootstrap, permissions, and profile access flows.

- `pages/`: login, forgot password, access denied
- `components/`: auth-only UI pieces
- `services/`: auth API and session orchestration
- `types/`: auth models and payloads