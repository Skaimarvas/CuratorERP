# Features

Each ERP domain should live in its own feature folder.

Recommended feature shape:

```text
features/<feature>/
  components/
  pages/
  services/
  types/
```

Keep routing, pages, local UI, feature services, and domain contracts together.