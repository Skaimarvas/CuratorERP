# Finance Feature

Use this feature for invoicing review, approvals, ledgers, settlements, and reporting.

- `pages/`: finance screens
- `components/`: finance-specific UI
- `services/`: finance API access and process orchestration
- `types/`: ledger, payment, approval, and report models