# Shared

Use `shared` for reusable building blocks with no feature ownership.

- `ui/`: reusable presentational components
- `forms/`: form helpers and reusable form controls
- `directives/`: shared directives
- `pipes/`: shared pipes
- `utils/`: pure helper functions
- `types/`: shared utility types used by multiple features